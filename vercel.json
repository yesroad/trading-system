{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "corepack enable && corepack prepare yarn@4.9.2 --activate && corepack yarn install --immutable",
  "buildCommand": "corepack enable && corepack prepare yarn@4.9.2 --activate && while [ ! -f turbo.json ] && [ \"$PWD\" != \"/\" ]; do cd ..; done && corepack yarn build --filter=web..."
}
